import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { MenumodalPage } from './menumodal';

@NgModule({
  declarations: [
    MenumodalPage,
  ],
  imports: [
    IonicPageModule.forChild(MenumodalPage),
  ],
})
export class MenumodalPageModule {}
