import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { sortmodalPage } from './sortmodal';

@NgModule({
  declarations: [
    sortmodalPage,
  ],
  imports: [
    IonicPageModule.forChild(sortmodalPage),
  ],
})
export class sortmodalPageModule {}
